(function (angular) {
	'use strict';

	/*
	 * Dependencies to imports.
	 */
	var imports = [
		'contactsModule'
	];

	/*
	 * Main module = App.
	 */
	var app = angular.module('app', imports);

})(window.angular);